import SwiftUI

struct V_Camera: View {
    var body: some View {
        Text("Hello, World!")
            .padding()
    }
}

struct V_Camera_Preview: PreviewProvider {
    static var previews: some View {
        V_Camera()
    }
}
